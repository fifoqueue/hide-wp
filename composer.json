{
  "name": "fifoqueue/hide-wp-surface",
  "description": "Reduces exposed WordPress paths and removable HTML fingerprints with verified server-side aliases.",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "require": {
    "php": ">=8.3"
  },
  "config": {
    "allow-plugins": false,
    "sort-packages": true
  }
}
